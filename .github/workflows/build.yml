name: Mobile Cloud Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    # 👇 THIS BYPASSES THE SYSTEM BUILDER AND FORCES THE RIGHT ENGINE STRAIGHT AWAY
    - name: Setup Gradle Environment
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.10.2

    # 👇 WE BUILD DIRECTLY WITHOUT REQUIRING THE WRAPPER FILES IN YOUR CODE
    - name: Direct Build APK
      run: gradle assembleDebug

    - name: Save APK Asset
      uses: actions/upload-artifact@v4
      with:
        name: my-app-apk
        path: app/build/outputs/apk/debug/app-debug.apk
